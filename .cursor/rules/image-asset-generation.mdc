---
description: 合成设计图与原子素材同步产出规范（禁止瞎生成；须 frontend-design）
alwaysApply: true
---

# 图片素材产出规范

## 硬纪律（先读再动手）

- **已定稿原子默认只读**：`docs/design/atoms/` 与小程序已同步素材，禁止为「调色 / 去毛刺 / 试一下」反复重生成、整图重绘、脚本乱改造型。
- **能 CSS 解决的不许生图**：圆底、渐变、鹅卵石鼓边、选中环、chip 等一律用 token / 样式（见 `docs/design/PLAN.md` §2.1、§2.1.1）；禁止把控件整合成一张 PNG 替代样式。
- **用户未明确要求生图时禁止调用生图工具**（含 `GenerateImage`、批量重绘、无授权的 chroma/重导出流水线）。
- **必须生图时**：先读并遵守 Cursor skill **`frontend-design`**（`~/.cursor/skills/frontend-design/SKILL.md`），再按下方原子流程执行；交付前对照 `docs/design/PLAN.md` / `CONTENT.md` UI 规范自检。
- **最小改动**：仅去毛刺、居中、去背、缩边同步时，必须以**现有原子或用户指定参考图**为唯一源，禁止换造型、换配色、换构图。
- **覆盖必留痕**：覆盖任何已有原子 / 分包 PNG 之前，必须先备份旧文件，禁止直接盖掉导致无法找回。

## 播放钮（定稿结构，禁止整图替代）

- **结构固定**：CSS 绿底（`--btn-play`）+ 鹅卵石鼓边（`--shadow-play`）+ **奶油黏土小三角**原子 `play.png`（一图一三角，透明底）。
- **禁止**：生成「绿盘+三角」整钮 PNG；禁止用勾选/其他图标顶替播放三角；禁止为对齐主题绿而重绘三角造型。
- **图标构图**：`play.png` / `stop.png` / `check.png` / `arrow.png` 主体铺满画布（约占边长 ≥90%），暖白黏土，禁止四周大留白。
- **切题箭头**：奶油黏土箭头 `arrow.png`（朝右）放在底栏左右矮条卡里，上一题 CSS `scaleX(-1)`；禁止绿胶囊文案钮、「圆盘+箭头」整钮 PNG。
- **长播停止**：古诗「整首诗」与字母歌大按钮播放中使用奶油黏土方块 `stop.png`（同材质、同体量），禁止用勾选或其他图标顶替。
- 三角/方块有毛刺或偏移：只对**现有对应原子**做去脏边 / 画布居中 / 预乘缩图同步，不另起炉灶。

## 原子与合成流程

- 生成任何包含多个视觉元素的合成图前，先逐个生成并保存其中可复用的原子素材。
- 原子素材必须一图一主体，使用透明背景；若生成工具不支持透明通道，使用纯色幕布并立即去背，确认透明效果后再参与合成。
- 合成图只能使用已归档的原子素材组合，不得先生成合成图再从中裁切或抠取素材。
- 原子素材和合成图都应使用清晰、稳定、可追踪的文件名，并归档到项目对应素材目录。
- 每次交付合成图时，同时确认对应原子素材已完整保存。
- 去背默认 `scripts/chroma_to_png.py`；归一化 `scripts/normalize_atoms.py`；进小程序用 `scripts/sync_package_images.py`（勿手写有损乱压）。

## 覆盖留痕（强制）

覆盖 `docs/design/atoms/*.png` 之前先跑：

```bash
python3 scripts/backup_atom.py docs/design/atoms/某文件.png
```

旧图落到 `docs/design/atoms/_history/{文件名不含扩展名}/{YYYYMMDD-HHmmss}.png`。生图进原子、chroma 覆盖、git restore 之外的回滚，都只动 `_history` 里的副本，不要丢掉时间戳目录。

## 动物素材统一规范

- 首页及功能入口动物统一为正面坐姿黏土造型：大头、小身体、圆润四肢、黑色豆豆眼、低饱和马卡龙配色、柔和腮红、哑光软陶质感。
- 每只功能动物必须双手或双翼抱持一个与模块直接相关的小道具；道具贴近胸前、尺寸小于动物头部，不得并排摆放在动物身旁或挤压动物主体尺寸。
- 道具只表达一个功能语义，造型简洁、无复杂文字；确需文字时只保留一个低幼可识别字符，例如拼音卡上的「a」。
- 动物应占画面主体，视觉体量、坐姿基线和头身比例与现有动物库一致；生成后必须通过 `scripts/chroma_to_png.py` 去背，再用 `scripts/normalize_atoms.py` 归一化。
- 功能与道具映射保持稳定：古诗—书、识字—字卡、算术—算盘、英语—字母积木/书、拼音—拼音卡/字母（首页入口可持卡；详情页用纯黏土单韵母字母图）、日历—日历牌、每日任务—星星清单、积分商城—星星/动物贴纸。
- 题库与视觉定稿见 `docs/design/CONTENT.md`；拼音详情仅 `a o e i u ü` 纯字母，不用动物拿卡。
