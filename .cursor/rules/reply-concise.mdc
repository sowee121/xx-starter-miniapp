---
description: 省 token：短回复、只出变更片段、少工具；拿不准先问；禁止重复产物
alwaysApply: true
---

# 回复与执行

少文字。改代码时给变更片段，不要全量文件（除非用户要完整文件）。不输出思考过程。不铺垫、不客套、不复述需求、不写「下面是代码」。禁止多余 Markdown 分隔线/大标题（用户要文档除外）。

## 回复格式

- 一句话概括改动；不可省：H5 / 小程序是否都改、关键数值、有意没做的
- 用户纠正后立刻改，不辩解、不重讲经过
- 长文件：标路径 + 只贴改动片段，省略未改部分

```
file: miniprogram/subpkg/parent/index.wxss
.clear-icon-slot { transform: scale(1.3); }
```

对：`家长区恐龙、独角兽改回 scale(1.3)；首页头像仍是 1.2。H5 和小程序都改了。`

错：`好的我理解了。我先检查文件再帮你改。`

## 执行

- 先读现有目录与风格，只改该改的；不重构无关代码
- 先用上下文已有内容；没有把握再搜，搜完就改
- 能并行的工具并行；改个数值不要开子代理、切模式、写长 todo
- 只读要改的文件；不为交差做全仓扫描、无关测试、浏览器巡检
- 能 `StrReplace` 就不要整文件重写；能改现有文件就不要新建
- 覆盖已有 PNG 前先 `python3 scripts/backup_atom.py <原子路径>`
- 样式任务才跑 `npm test`；纯文案/规则/单值不必

## 工程质量（防返工）

- 分包体积、主包/分包边界、图片不跨包；禁止 WebP；iOS / 安卓都要能显示
- 不引入重型第三方包；优先现有工具链（chroma / sync）
- 修 bug：根因一句话 + 最小修复片段，不大范围重写
- 性能任务：可落地的代码/配置片段 + 短清单，不抒情

## 禁止重复产物

- 不要第二份 PNG / 规则 / 组件 / 页面 / slot
- 改数值或文案就改现有文件
- 词卡/头像：能复用就复用；head 已是那张图就不要再复制一张 identical face，除非用户确认共用

## 拿不准就问

只提 1–2 个最关键问题，不连环追问。

- 两种改法都说得通
- 「按相同模式」可能指结构或数值
- 会动定稿原子、跨词条复用图、可能误伤其它模块

## 出图

用户没点头前不要 GenerateImage。先用一两句话写死造型/颜色/角度，等确认再生一版。批量生图前，先出一张样图给用户看看，确认可以再批量生成。
