---
description: H5 审查稿只做静态 UI，禁止点读/点击等产品交互
alwaysApply: true
---

# H5 审查稿 = 静态 UI

`docs/design/h5/` 全站（首页、八大板块、家长区、反馈合页等）**只展示视觉**，对齐小程序外观。

## 必须

- H5 只出静态 HTML + CSS 帧：布局、色调、间距、文案、素材
- 点读、切题、进页自动播、播放/停止、答题、打卡、兑换等**产品行为只做小程序**
- 用户在 H5 预览里圈选节点，只当作「改这处视觉」；不要据此给 H5 加交互

## 允许：纯页面跳转

H5 **允许纯导航**：只能是跳到本目录另一张静态帧，不带业务参数。

- 写法任选：原生 `<a href="...">`，或 `docs/design/h5/js/nav.js`（事件委托，按「页面 + 入口选择器」集中映射，改 DOM 结构和 CSS 都不用动）
- 映射跟随小程序真实导航路径；列表条目统一跳该板块已有的示例详情页即可
- 新增页面记得在页尾引入 `js/nav.js`，新入口加进 `PAGES` 映射表

## 禁止

- 给 H5 加 `Audio` / `<audio>`、进页自动播放、点读
- 给 H5 加点击切换、切题、答题、表单提交、长按手势、兑换、打卡等产品逻辑
- 为预览方便把小程序音频拷进 `docs/design/h5/`
- 用「审查页也要点一下听听」给 H5 写脚本

家长区清除槽等**仅为演示态的 CSS 动画**（无业务副作用）除外；不要扩成可玩的 H5 应用。
