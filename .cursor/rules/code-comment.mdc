---
description: 代码注释规范：函数与模块级变量上方必须 /** 摘要 */，关键逻辑行上方加 // 说明
alwaysApply: true
---

# 代码注释规范

注释统一用**简体中文**。写「为什么这么做」，不写「这行在干什么」——代码本身能说明的不要重复。

## 必须加

| 对象 | 形式 | 示例 |
| --- | --- | --- |
| 模块级函数 | 上一行 `/** 摘要 */` | `/** 读本地进度表 */` |
| 模块级 `const` / `let`（常量、状态变量） | 上一行 `/** 摘要 */` | `/** 冲刷中，禁止并发重入 */` |
| 语义不直观的对象方法 / 事件回调 | 上一行 `/** 摘要 */` | `/** 长按多久算确认清除 */` |
| 函数内部关键逻辑行 | 上一行 `// 说明` | `// stop 后不要立刻改 src；换源由 play() 自己延迟推进` |

关键逻辑行指：分支判断的理由、异步时序、平台兼容 hack、非直觉的兜底、跨端/跨包约束。

## 不需要加

- **生命周期**：`onLoad` / `onShow` / `onReady` / `attached` / `detached` 等，一目了然
- **函数内局部变量**
- **`require` 行**：`const audioUtil = require('../../utils/audio')` 加注释是废话
- **模板式箭头函数属性**：`titleOf: (n) => \`读 ${n} 首古诗\`` 这类配置
- **一眼看懂的内联回调**：`onEnded` / `onError` 里只有一行转发时
## 格式统一

- **声明上方（函数 / 模块级 const / let / 对象属性）一律 `/** 摘要 */`**，包括 `require` 行若写理由也在上一行用 `/** */`。
- **语句上方、函数内部逻辑说明一律 `//`**（如分支判断理由、异步时序、平台 hack、兜底）。
- 不要把两类混用：`//` 只给"这一行在干嘛 / 为什么这么写"，`/** */` 只给"下面的声明是什么"。

例外：`require` 上方若有「为什么这么引」的理由（如静态引入避免被 catch 吞掉），**必须**写在上一行。

## 写法

1. 单行用 `/** 摘要 */`，**不要**拆成多行块注释，除非确实需要交代背景。
2. 需要交代背景时用多行，说明取舍与后果：

```js
/**
 * 防御上限：长期离线时避免队列无限堆积挤占存储。
 * 按 module::itemId 去重，正常使用远不会触发；裁剪会丢「已学未同步」记录，故取值比星星队列宽。
 */
const MAX_SIZE = 500
```

3. 魔数必须解释取值理由：`/** 长按多久算确认清除；够长才能挡住宝宝误触 */`。
4. 与另一端（云端 / 其它模块 / wxss）耦合的常量，注释里写明**和谁同步**：
   `/** 热力保留窗口（月），与云端 bumpHeat 的 prune 必须一致 */`。
5. `@param` / `@returns` 不是必须，仅在参数含义不看名就懂不了时才加（参考 `utils/navbar.js`）。
6. `catch` 空块统一写 `// ignore`（全仓唯一写法，别再写 `// 静默` / `// 忽略`），有原因的接在冒号后：`// ignore：下次上报 / 页面进入时会再同步`（见 `utils/daily-tasks.js`）；**不要**留空。

## 禁止

- 注释掉的代码块直接删掉，不要留在仓库里
- `TODO` / `FIXME` 不入库，要么当次做完，要么记到需求文档
- 用 `/* ---------- 分块 ---------- */` 之类的装饰性分隔线（`utils/daily-tasks.js` 一处历史遗留，可保留但别新增）
- 注释与代码不一致时，**改注释**
- 跨文件逐字复制的长注释块：公共逻辑（如幂等建用户、并发竞态）注释精炼成一行摘要，不要多个云函数各贴一遍同款 5 行块
