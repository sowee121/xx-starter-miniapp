---
description: 标准 Git 提交必须走完 pull → commit → push，只在当前 main 操作
alwaysApply: true
---

# Git 提交流程

用户说「提交」即要求走完整流程，不得只 commit 不 push，也不得跳过 pull。

## 必须按此顺序

1. 只在当前 `main` 操作，不另开分支、不用 worktree 旁路提交。
2. `git pull origin main`，先与远端对齐。
3. `git status` / `git diff` / `git log` 确认改动。
4. 以**本地工作区最新代码**为准 `git add` 并 `git commit`。
5. `git push origin main`。
6. 再看 `git status`，确认已与 `origin/main` 对齐。

## 禁止

- 跳过 pull 或 push
- 为了提交去对齐一份「远端干净树」而丢掉或绕开工作区代码
- 未要求时 `reset --hard`、乱开分支、把提交做到别的分支

若 pull 出现冲突：停下来解决冲突，解决后再 commit、push，禁止用远端覆盖本地未提交改动。
