.sticker-grid {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  box-sizing: border-box;
  width: 100%;
  gap: var(--gap-grid);
}

.album-hint {
  width: 100%;
  text-align: center;
  font-size: var(--font-hint);
  color: var(--muted);
}
