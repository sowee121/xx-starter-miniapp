<app-shell title="识字" stars="{{stars}}">
  <view wx:if="{{item}}" class="detail-stack">
    <view class="detail-big block tone-{{tone}}" data-kind="char" bindtap="play" hover-class="is-pressed" hover-start-time="0" hover-stay-time="80">
      <view class="detail-emoji">{{item.emoji}}</view>
      <view class="detail-big__glyph">{{item.char}}</view>
      <view class="detail-big__sub">{{item.pinyin}} · {{item.strokes}} 画</view>
      <play-button size="lg" passive="{{true}}" />
    </view>
    <view class="detail-pair">
      <view
        wx:for="{{item.words}}"
        wx:for-item="word"
        wx:for-index="widx"
        wx:key="*this"
        class="detail-pair__card block tone-cream"
        data-kind="word"
        data-index="{{widx}}"
        bindtap="play" hover-class="is-pressed" hover-start-time="0" hover-stay-time="80">
        <view>{{word}}</view>
        <play-button compact="{{true}}" size="sm" passive="{{true}}" />
      </view>
    </view>
    <trail-nav
      show="{{nav.total > 1}}"
      has-prev="{{nav.hasPrev}}"
      has-next="{{nav.hasNext}}"
      bind:prev="goPrev"
      bind:next="goNext"
    />
    <view wx:if="{{softNote}}" class="soft-note block tone-butter">{{softNote}}</view>
  </view>
  <praise-sun
    show="{{feedback.show}}"
    closing="{{feedback.closing}}"
    variant="{{feedback.variant}}"
    title="{{feedback.title}}"
    desc="{{feedback.desc}}"
    action="{{feedback.action}}"
    bind:continue="closePraise"
  />
</app-shell>
