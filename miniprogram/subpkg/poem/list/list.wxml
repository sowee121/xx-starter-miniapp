<app-shell title="古诗" hint="选一首古诗" sub-hint="听一听，读一读" stars="{{stars}}">
  <view class="poem-list page-list">
    <media-card
      wx:for="{{poems}}"
      wx:key="id"
      data-id="{{item.id}}"
      bind:tap="onOpen"
      title="{{item.title}}"
      subtitle="{{item.author}}"
      image="{{item.icon}}"
      tone="{{item.tone}}"
      play="{{true}}"
      wide-image="{{true}}"
    />
  </view>
</app-shell>
