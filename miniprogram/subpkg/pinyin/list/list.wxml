<app-shell title="拼音" hint="单韵母" sub-hint="听一听，学发音" stars="{{stars}}">
  <view class="word-grid">
    <word-card
      wx:for="{{items}}"
      wx:key="id"
      image="{{item.image}}"
      cls="tone-cream"
      data-id="{{item.id}}"
      bindtap="open"
    />
  </view>
</app-shell>
