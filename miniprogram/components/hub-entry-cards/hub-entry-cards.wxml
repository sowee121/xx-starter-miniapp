<view class="hub-entry-cards">
  <hub-entry-card
    wx:for="{{cards}}"
    wx:key="to"
    image="{{item.image}}"
    title="{{item.title}}"
    sub="{{item.sub}}"
    tone="{{item.tone}}"
    to="{{item.to}}"
    bind:go="onCardGo"
  />
</view>
