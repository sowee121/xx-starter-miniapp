<view
  class="custom-header {{variant === 'home' ? 'is-home' : 'is-page'}} {{night ? 'is-night' : ''}}"
  style="height: {{headerHeight}}px;"
>
  <view
    class="custom-header__row"
    style="top: {{menuTop}}px; height: {{menuHeight}}px; padding-right: {{menuRight + 12}}px;"
  >
    <block wx:if="{{variant === 'home'}}">
      <view class="custom-header__brand">{{title}}</view>
      <view class="custom-header__right">
        <star-bar stars="{{stars}}" night="{{night}}" capsuleHeight="{{menuHeight}}" />
      </view>
    </block>
    <block wx:else>
      <view
        wx:if="{{showHome}}"
        class="custom-header__home motion-tap"
        bindtap="onHome"
        hover-class="is-pressed"
        hover-start-time="0"
        hover-stay-time="80"
      >
        <image class="custom-header__home-icon" src="{{homeIcon}}" mode="aspectFit" />
      </view>
      <view wx:else class="custom-header__home-spacer"></view>
      <view class="custom-header__title">{{title}}</view>
      <view class="custom-header__page-stars">
        <star-bar stars="{{stars}}" night="{{night}}" capsuleHeight="{{menuHeight}}" />
      </view>
    </block>
  </view>
</view>
