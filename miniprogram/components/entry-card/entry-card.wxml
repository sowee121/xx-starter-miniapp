<view
  class="entry-card motion-tap entry-card--{{tone}}"
  bindtap="onTap"
  hover-class="is-pressed"
  hover-start-time="0"
  hover-stay-time="80"
>
  <image class="entry-card__image" src="{{image}}" mode="aspectFit" />
  <view class="entry-card__title">{{title}}</view>
  <view class="entry-card__sub">{{sub}}</view>
  <view class="entry-card__desc">{{desc}}</view>
</view>
