:host {
  /* 宿主做块级占位，宽度由父容器 .entry-grid__cell 控制 */
  display: block;
}

/* 单张入口卡（english/hub、math/hub 共用），单列整行大卡，对齐 H5 blocks.css .entry-card */

.entry-card {
  position: relative;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 100%;
  /* 56 padding + 240 图 + 组间 24 + 文字组（50+12+47+12+45）≈ 486，取整留余 */
  min-height: 490rpx;
  gap: var(--gap-inset);
  padding: var(--pad-card);
  text-align: center;
  border-radius: var(--radius-card);
  box-sizing: border-box;
}

/* 图 / 文字两区块：组间 gap 走外层 gap-inset，三行文字在组内收细为 gap-text */
.entry-card__text {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--gap-text);
}

/* 色面：取值对齐全局 tone-sky/rose/sand；新增色调需在此补充 */
.entry-card--sky {
  background: var(--tone-sky);
  box-shadow: var(--shadow-tone-sky);
}

.entry-card--rose {
  background: var(--tone-rose);
  box-shadow: var(--shadow-tone-rose);
}

.entry-card--sand {
  background: var(--tone-sand);
  box-shadow: var(--shadow-tone-sand);
}

.entry-card__image {
  width: 240rpx;
  height: 240rpx;
}

.entry-card__title {
  font-size: var(--font-note);
  font-weight: 700;
  color: var(--ink);
  line-height: 1.2;
}

.entry-card__sub {
  font-size: var(--font-title);
  color: var(--muted);
  line-height: 1.3;
}

.entry-card__desc {
  font-size: var(--font-hint);
  color: var(--muted);
  line-height: 1.4;
}
