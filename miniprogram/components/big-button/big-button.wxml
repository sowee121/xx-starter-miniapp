<view class="big-btn custom-class {{disabled ? 'is-disabled' : ''}}" bindtap="onTap">
  <image wx:if="{{showPlay}}" class="big-btn__icon" src="{{playIcon}}" mode="aspectFit" />
  <view class="big-btn__text">{{text}}</view>
</view>
