/* 尺寸与渐变来自全局 .big-btn */
.big-btn {
  gap: var(--gap-soft);
}

.big-btn__icon {
  width: 46rpx;
  height: 46rpx;
  flex-shrink: 0;
}

.big-btn__text {
  color: #ffffff;
  font-size: var(--font-card);
  font-weight: 700;
  line-height: 1.2;
  letter-spacing: var(--letter-btn);
}
