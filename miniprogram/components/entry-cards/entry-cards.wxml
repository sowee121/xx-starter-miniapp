<view class="entry-cards">
  <view wx:for="{{cards}}" wx:key="to" class="entry-cards__cell">
    <entry-card
      image="{{item.image}}"
      title="{{item.title}}"
      sub="{{item.sub}}"
      tone="{{item.tone}}"
      to="{{item.to}}"
      bind:go="onCardGo"
    />
  </view>
</view>
